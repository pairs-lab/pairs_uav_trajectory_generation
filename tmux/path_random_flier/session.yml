# do not modify these
root: ./
name: simulation
socket_name: pairs
attach: false
tmux_options: -f /etc/ctu-pairs/tmux.conf
# you can modify these
startup_window: random_flier
pre_window: export UAV_NAME=uav1;
            export UAV_TYPE=x500;
            export PLATFORM_CONFIG=`ros2 pkg prefix pairs_multirotor_simulator`/share/pairs_multirotor_simulator/config/pairs_uav_system/$UAV_TYPE.yaml;
            export USE_SIM_TIME="true";
            export RMW_IMPLEMENTATION=rmw_zenoh_cpp
windows:
  - router:
      layout: tiled
      panes:
        - ros2 run rmw_zenoh_cpp rmw_zenohd
  - simulator:
      layout: tiled
      panes:
        - ros2 launch pairs_multirotor_simulator multirotor_simulator.launch.py custom_config:=./config/simulator.yaml
  - hw_api:
      layout: tiled
      panes:
        - ros2 launch pairs_multirotor_simulator hw_api.launch.py
  - core:
      layout: tiled
      panes:
        - ros2 launch pairs_uav_core core.launch.py
          platform_config:=$PLATFORM_CONFIG
          world_config:=./config/world_config.yaml
          custom_config:=./config/custom_config.yaml
          network_config:=./config/network_config.yaml
  - status:
      layout: tiled
      panes:
        - ros2 run pairs_uav_status status.sh
  - takeoff:
      layout: tiled
      panes:
        - ros2 launch pairs_uav_autostart automatic_start.launch.py custom_config:=./config/automatic_start.yaml
        - waitForCore && ./takeoff.sh
  - commands:
      layout: tiled
      panes:
        - waitForTakeoff && ./goto.py
  - random_flier:
      layout: tiled
      panes:
        - ros2 launch pairs_uav_trajectory_generation path_random_flier.launch.py custom_config:=./config/path_random_flier.yaml
  - rviz:
      layout: tiled
      panes:
        - ros2 run rviz2 rviz2 -d ./rviz.rviz
  - layout:
      layout: tiled
      panes:
        - waitForCore; ~/.i3/layout_manager.sh ./layout.json
